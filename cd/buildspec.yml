version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      - nest build
      - pwd
    run-as: root
artifacts:
  files:
    - "**/*"
cache:
  paths:
    - "node_modules/**/*"