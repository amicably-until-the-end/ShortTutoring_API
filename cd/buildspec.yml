version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      - pwd
    run-as: root
  build:
    commands:
      - nest build
artifacts:
  files:
    - "**/*"
cache:
  paths:
    - "node_modules/**/*"